const key = 'events_access_token'; const notice = document.querySelector('#notice');
const callbackToken = new URLSearchParams(window.location.hash.slice(1)).get('access_token'); if (callbackToken) { localStorage.setItem(key, callbackToken); history.replaceState(null, '', window.location.pathname); }
const api = async (path, options = {}) => { const response = await fetch(path, { ...options, headers: { 'content-type': 'application/json', authorization: `Bearer ${localStorage.getItem(key)}` } }); const data = await response.json(); if (!response.ok) { const error = Error(data.message); error.status = response.status; throw error; } return data; };
const message = (text, error = false) => { notice.textContent = text; notice.className = `notice ${error ? 'error' : ''}`; }; const clearError = () => { if (notice.classList.contains('error')) { notice.textContent = ''; notice.className = 'notice hidden'; } }; const date = (value) => new Date(value).toLocaleString('pl-PL');
const resetAdminView = () => { localStorage.removeItem(key); document.querySelector('#dashboard').classList.add('hidden'); document.querySelector('#login').classList.remove('hidden'); document.querySelector('#events').textContent = ''; document.querySelector('#users').textContent = ''; };
const switchTab = (name) => { document.querySelectorAll('[data-tab]').forEach((button) => button.classList.toggle('active', button.dataset.tab === name)); document.querySelectorAll('.tab-panel').forEach((panel) => panel.classList.toggle('active', panel.id === `${name}-tab`)); };
document.querySelectorAll('[data-tab]').forEach((button) => { button.onclick = () => switchTab(button.dataset.tab); });
async function refresh() { try { const { user } = await api('/api/auth/me'); if (user.role !== 'admin') throw Error('Konto nie ma roli administratora.'); clearError(); document.querySelector('#login').classList.add('hidden'); document.querySelector('#dashboard').classList.remove('hidden'); const [{ events }, { users }] = await Promise.all([api('/api/admin/events'), api('/api/admin/users')]); const enriched = await Promise.all(events.map(async (event) => ({ event, registrations: (await api(`/api/admin/events/${event.id}/registrations`)).registrations })));
document.querySelector('#events').innerHTML = enriched.map(({ event, registrations }) => `<article class="card"><h3>${event.name}</h3><p class="meta">${date(event.eventDatetime)} · ${event.registeredCount}/${event.capacity} miejsc · ${event.status}</p><h4>Uczestnicy (${registrations.length})</h4>${registrations.length ? `<ul class="participants">${registrations.map((registration) => `<li>${registration.profiles.full_name} <span>${registration.profiles.email}</span></li>`).join('')}</ul>` : '<p class="meta">Brak zapisanych użytkowników.</p>'}<div class="actions"><button data-action="status" data-id="${event.id}" data-status="${event.status}">${event.status === 'current' ? 'Archiwizuj' : 'Przywróć'}</button><button class="danger" data-action="reset" data-id="${event.id}">Reset zapisów</button>${event.status === 'archived' && !registrations.length ? `<button class="danger" data-action="delete" data-id="${event.id}">Usuń trwale</button>` : ''}</div></article>`).join('') || '<p>Brak wydarzeń.</p>';
document.querySelector('#users').innerHTML = `<table><tr><th>Imię i nazwisko</th><th>E-mail</th><th>Rola</th><th>Status</th><th>Akcje</th></tr>${users.map((userRow) => `<tr><td>${userRow.full_name}</td><td>${userRow.email}</td><td>${userRow.role}</td><td>${userRow.is_active ? 'Aktywne' : 'Nieaktywne'}</td><td><div class="actions"><button class="secondary" data-user-action="${userRow.is_active ? 'deactivate' : 'restore'}" data-id="${userRow.id}">${userRow.is_active ? 'Dezaktywuj' : 'Przywróć'}</button><button class="danger" data-user-action="delete" data-id="${userRow.id}">Usuń trwale</button></div></td></tr>`).join('')}</table>`;
document.querySelectorAll('#events button').forEach((button) => { button.onclick = async () => { try { if (button.dataset.action === 'status') await api(`/api/admin/events/${button.dataset.id}/${button.dataset.status === 'current' ? 'archive' : 'restore'}`, { method: 'POST' }); if (button.dataset.action === 'reset') { if (!confirm('Usunąć wszystkie zapisy?')) return; await api(`/api/admin/events/${button.dataset.id}/reset`, { method: 'POST' }); } if (button.dataset.action === 'delete') { if (!confirm('Trwale usunąć to wydarzenie?')) return; await api(`/api/admin/events/${button.dataset.id}`, { method: 'DELETE' }); } message('Zapisano zmianę.'); refresh(); } catch (error) { if ([401, 403].includes(error.status)) resetAdminView(); message(error.message, true); } }; });
document.querySelectorAll('[data-user-action]').forEach((button) => { button.onclick = async () => { try { const action = button.dataset.userAction; if (action === 'delete') { if (!confirm('Trwale usunąć konto i powiązane zapisy?')) return; await api(`/api/admin/users/${button.dataset.id}`, { method: 'DELETE' }); } else await api(`/api/admin/users/${button.dataset.id}/${action}`, { method: 'POST' }); message('Zapisano zmianę.'); refresh(); } catch (error) { if ([401, 403].includes(error.status)) resetAdminView(); message(error.message, true); } }; });
} catch (error) { resetAdminView(); message(error.message, true); } }
document.querySelector('#login-form').onsubmit = async (event) => { event.preventDefault(); try { const data = await api('/api/auth/login', { method: 'POST', body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); localStorage.setItem(key, data.accessToken); refresh(); } catch (error) { message(error.message, true); } };
document.querySelector('#magic').onclick = async () => { try { const email = new FormData(document.querySelector('#login-form')).get('email'); await api('/api/auth/magic-link', { method: 'POST', body: JSON.stringify({ email }) }); message('Link logowania został wysłany.'); } catch (error) { message(error.message, true); } };
document.querySelector('#reset').onclick = async () => { try { const email = new FormData(document.querySelector('#login-form')).get('email'); await api('/api/auth/password-reset', { method: 'POST', body: JSON.stringify({ email }) }); message('Jeżeli konto istnieje, wysłaliśmy link do ustawienia nowego hasła.'); } catch (error) { message(error.message, true); } };
document.querySelector('#event-form').onsubmit = async (event) => { event.preventDefault(); try { const data = Object.fromEntries(new FormData(event.target)); data.capacity = Number(data.capacity); data.eventDatetime = new Date(data.eventDatetime).toISOString(); await api('/api/admin/events', { method: 'POST', body: JSON.stringify(data) }); event.target.reset(); message('Utworzono wydarzenie.'); refresh(); } catch (error) { if ([401, 403].includes(error.status)) resetAdminView(); message(error.message, true); } };
document.querySelector('#user-form').onsubmit = async (event) => { event.preventDefault(); try { await api('/api/admin/users', { method: 'POST', body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); event.target.reset(); message('Wysłano zaproszenie.'); refresh(); } catch (error) { if ([401, 403].includes(error.status)) resetAdminView(); message(error.message, true); } };
document.querySelector('#password-form').onsubmit = async (event) => { event.preventDefault(); try { await api('/api/auth/change-password', { method: 'POST', body: JSON.stringify(Object.fromEntries(new FormData(event.target))) }); event.target.reset(); message('Hasło zostało zmienione.'); } catch (error) { if ([401, 403].includes(error.status)) resetAdminView(); message(error.message, true); } };
if (localStorage.getItem(key)) refresh();
